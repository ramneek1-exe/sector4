# Python
.venv/
venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# fastf1 cache and intermediate data (do not commit) — root-anchored so src/data/ is tracked
/cache/
/data/

# Secrets / env
.env
.env.local
*.local

# Node / Next.js (for later phases)
node_modules/
.next/
out/
.vercel/
next-env.d.ts
*.tsbuildinfo

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Claude Code local state
.claude/
.agents/
skills-lock.json
.vercel

# Visual-companion brainstorm scratch
.superpowers/

# Lastik desktop font originals — only the web .woff2/.woff are tracked
/app/fonts/lastik/Lastik-Regular.otf
/app/fonts/lastik/Lastik-Regular.ttf
