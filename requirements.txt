# Sector 4 — runtime deps for the Vercel Python /api function (the lookup path).
# Keep this MINIMAL: the function must fit Vercel's 500MB Python Lambda limit, so
# list only what api/inference.py -> src.inference.lookup needs at import/runtime.
# Everything heavier (fastf1, scikit-learn, scipy, matplotlib, pytest) lives in
# requirements-dev.txt for local dev, the batch pipeline, and the test suite.
pandas
pyarrow          # parquet IO for the lookup tables (tyre_deg / stint_length)
numpy            # transitively via pandas; listed explicitly for clarity
